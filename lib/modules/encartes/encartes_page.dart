import 'package:flutter/material.dart';

class EncartesPage extends StatelessWidget {
  const EncartesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Encartes')),
      body: const Center(child: Text('Tela de Encartes')),
    );
  }
}
